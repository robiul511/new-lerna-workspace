{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "install:bootstrap": "yarn install && lerna bootstrap",
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run dev",
    "dev:app-vue": "lerna run dev --scope=app-vue"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
